spring.application.name=paymybuddy

# MySQL database connectivity
spring.datasource.url=jdbc:mysql://localhost:3306/paymybuddy
spring.datasource.username=paymybuddy
spring.datasource.password=lutho1904.mysql
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver
spring.security.user.name=user
spring.security.user.password=12345

# Hibernate/JPA settings
# 'update' automatically updates the database schema based on your entities
# other options include 'create', 'create-drop', and 'none'
spring.jpa.hibernate.ddl-auto=update

# Show SQL queries in the console (optional, good for development)
spring.jpa.show-sql=true

# Server port (optional, default is 8080)
server.port=8080